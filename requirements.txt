# Streamlit Community Cloud installs from this file on every deploy, so it has
# to cover the app as well as the pipeline.

# Pipeline
pandas>=2.0
numpy>=1.24

# Streamlit app
streamlit>=1.49
plotly>=5.20

# Only used to regenerate the README screenshot crops
Pillow>=10.0
